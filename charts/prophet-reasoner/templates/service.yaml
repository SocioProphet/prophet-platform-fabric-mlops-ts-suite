apiVersion: v1
kind: Service
metadata:
  name: { include "prophet-reasoner.fullname" . }
  labels:
    app.kubernetes.io/name: prophet-reasoner
    app.kubernetes.io/part-of: prophet-platform
    socioprofit.com/atom: prophet-reasoner
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: prophet-reasoner
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
