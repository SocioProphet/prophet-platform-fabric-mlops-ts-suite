apiVersion: apps/v1
kind: Deployment
metadata:
  name: { include "prophet-control-plane.fullname" . }
  labels:
    app.kubernetes.io/name: prophet-control-plane
    app.kubernetes.io/part-of: prophet-platform
    socioprofit.com/atom: prophet-control-plane
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: prophet-control-plane
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prophet-control-plane
        app.kubernetes.io/part-of: prophet-platform
        socioprofit.com/atom: prophet-control-plane
    spec:
      containers:
        - name: prophet-control-plane
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: { include "prophet-control-plane.fullname" . }-config
          env:
            {- range $k, $v := .Values.env }
            - name: {{ $k }}
              value: {{ $v | quote }}
            {- end }
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
{{ toYaml .Values.resources | indent 20 }}
