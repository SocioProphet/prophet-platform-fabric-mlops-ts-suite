apiVersion: v1
kind: Service
metadata:
  name: { include "prophet-materializer-clickhouse.fullname" . }
  labels:
    app.kubernetes.io/name: prophet-materializer-clickhouse
    app.kubernetes.io/part-of: prophet-platform
    socioprofit.com/atom: prophet-materializer-clickhouse
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: prophet-materializer-clickhouse
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
