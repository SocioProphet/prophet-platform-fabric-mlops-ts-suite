apiVersion: v1
kind: ConfigMap
metadata:
  name: { include "prophet-agent.fullname" . }-config
  labels:
    app.kubernetes.io/name: prophet-agent
    app.kubernetes.io/part-of: prophet-platform
    socioprofit.com/atom: prophet-agent
data:
  # Add non-secret config here
  LOG_LEVEL: "info"
