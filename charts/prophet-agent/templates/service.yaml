apiVersion: v1
kind: Service
metadata:
  name: { include "prophet-agent.fullname" . }
  labels:
    app.kubernetes.io/name: prophet-agent
    app.kubernetes.io/part-of: prophet-platform
    socioprofit.com/atom: prophet-agent
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: prophet-agent
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
