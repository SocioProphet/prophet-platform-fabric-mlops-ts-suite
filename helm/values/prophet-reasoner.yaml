      image:
        repository: ghcr.io/socioprofit/prophet-reasoner
        tag: "0.1.0"
        pullPolicy: IfNotPresent

      service:
        type: ClusterIP
  port: 8086

      env:
# SP_REGION: "us-east-1"
# SP_LOG_LEVEL: "info"

      resources: {}
