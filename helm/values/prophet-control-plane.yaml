      image:
        repository: ghcr.io/socioprofit/prophet-control-plane
        tag: "0.1.0"
        pullPolicy: IfNotPresent

      service:
        type: ClusterIP
  port: 8080

      env:
# SP_REGION: "us-east-1"
# SP_LOG_LEVEL: "info"

      resources: {}
